# Vale for this repository. The rules, and the reasons for them, live in the
# Louise house package (louise-toolkit ADR 0013), which every Bowen Labs
# repository shares. This file only says where styles go and which packages to
# fetch. `vale sync` fills the gitignored .vale/ with both.
StylesPath = .vale
Packages = Google, https://github.com/bowenlabs/louise-toolkit/releases/download/vale-v1.1.0/Louise.zip
